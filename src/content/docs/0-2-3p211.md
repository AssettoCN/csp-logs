---
title: 0.2.3-preview211
translated_by: ai
---

*   版本 ID：2974
*   大小：??.?? MB
*   发布日期：2024-04-06

> ℹ️ 本页面由 AI 翻译，仅供参考。如与英文原版有出入，以 [英文版](/csp-logs/en/0-2-3p211/) 为准。

# 更新日志

## 新功能、选项与改进

*   SurfacesFX：替代砾石、沙地与草地物理的实验性模块（更有意思的东西即将推出）；
*   默认 WeatherFX 样式更新：
    *   可选的线性色彩空间：
        *   终于能正确渲染了；
        *   该选项激活时观感大幅重做；
        *   会考虑湿度与气压；
    *   雷暴中有真正的闪电；
    *   YEBIS 替代：
        *   默认/感光测定色调映射函数经过调整，更精确；
        *   自动曝光现在非常接近 YEBIS 版本；
*   离线比赛可用的新物理实验：
    *   所有车辆的扩展物理；
    *   额外的侧向接触点（对路肩有帮助，会把接触点左右移动，从而改变 FFB）；
    *   阳光阴影影响表面温度移入物理实验分区；
    *   AC 物理线程池的替代方案，把车辆计算分摊到各核心；
    *   替代的最佳制动计算（某些车轮未触地时改变 AI 与键盘转向的行为）；
    *   让程序化噪声更准确地影响多条轮胎检测线；
    *   将部分表面的正弦噪声替换为 perlin；
    *   让程序化噪声（正弦或 perlin）也影响接触法线；
*   鼠标转向：
    *   激活时，CSP 应用、F5 与座舱相机忽略鼠标；
    *   用 Lua 脚本更改行为的选项；
    *   用鼠标中键进行开关的选项；
*   Traffic Planner 及其模式：
    *   修复大型巴士靠近屏幕边缘时不渲染的问题；
    *   将线路导出为新的 AI 线路的选项；
    *   车辆新增 `chance` 字段，附带少量内置推测；
*   Track Adjustments（赛道调整）：
    *   胎沟贴合到底层网格，阴影不再叠加；
    *   实验性：为所有网格启用带偏置的双面阴影，防止脱节的阴影；
    *   实验性：修正配置不当的胎沟，使其仍绘制在 Fake shadows 之前；
*   New behavior 中的新选项：
    *   让 AI 在低于 80% 的油门开度下正确使用油门踏板；
    *   修正基于 RPM 限制器设置错误的升挡阈值（更高级的逻辑即将推出）；
    *   切换到安全的 AI 线路元数据光线检测（AI 线路正下方没有物理表面时不再崩溃）；
*   升挡与降挡的替代映射改由 CSP 处理，更可靠；
*   按住手刹一段时间后保持其咬合的选项；
*   实况天气控制器：新增 Open-Meteo 天气提供方；
*   几个已完成的物理实验不再是实验，处处默认启用；
*   Walking Out 模式：下车时保留车辆输入状态，包括转向角；
*   常规设置中为保险杠或引擎盖相机添加基于 G 力的轻微摇晃；
*   VR 调整：SPS 与后处理的几项修复、更改鼠标指针颜色的选项、让指针更快淡出的选项；
*   隐藏了不必要的设置，让界面更干净；
*   IMGUI 字体现在包含更多特殊符号；
*   新渲染统计应用：较慢平均值的选项，计时器的几项修复；
*   Smart Mirror：为主视口中不可见的后视镜显示虚拟镜像的选项；
*   照片模式应用：切换车辆污渍与损坏；
*   DXGI 调整：后台将 AC 限制到 30 FPS 的选项；
*   车手工具提示中显示其他车手的延迟（ping）；
*   FSR：使用自定义像素密度的选项，包括高于 100% 的值以获得更多细节；
*   CSP Debug：离线比赛中控制当前天气的更多方面；
*   GrassFX：跳过加载旧草地网格的选项，加快加载并降低内存占用；
*   聊天 TTS：使用多个语音的选项；
*   控制音频播放的新绑定；

## 扩展物理

*   通用：
    *   Cosmic 默认启用中心处的陀螺 FFB 衰减（市面上的 FFB 方向盘基本都需要）；
    *   低速物理（Sliding Fix）修改；
    *   Kunos 三次插值 LUT（轮胎载荷敏感度与倾角 LUT）不再外推（多年来给 mod 作者带来无数问题，包括莫名的爆炸与游戏崩溃）；
*   Cosmic 悬架：
    *   现在可以为 KS ARB 声明车身（自定义底盘柔性必须如此，否则始终使用 `front` 底盘，刚度不正确）；
    *   球关节获得可调几何（语法与距离关节相同）；
    *   Cosmic 车身柔性（使用 ARB_BODY 时）可在视觉上呈现（必须使用 USE_FOR_ARB；它是后底盘车身的标志）；
    *   调试线使用精确的关节位置；
*   轮胎：
    *   支持热模型 v2（文档即将推出）；
    *   更好的边界情况接触法线计算（把接触法线限制在偏离轮胎径向法线的某个阈值内；可避免检测线以奇怪方式接触表面时出现的诡异载荷尖峰等边界现象）；
    *   将松弛长度应用于力而非滑移率的选项（添加 `[_EXTENSION] USE_RELAX_LENGTH_ON_FORCE=1`）；
*   碰撞体：
    *   碰撞体新增可用的磨损函数（文档即将推出）；
    *   可访问 Lua 参数（每个碰撞体 `ALLOW_LUA_RESET=1` 可让 Lua 单独重置磨损，例如进站时）；
*   悬架：
    *   减震器 LUT 在 Car Debug 应用中恢复正确显示；
*   刹车：
    *   刹车热模型考虑 ABS（原版物理也有同样问题，但为兼容性不会修复；感谢 Tuttertep 报告）；

## 修复

*   修复了使用 Cosmic 悬架的远程车辆车轮位置；
*   ExtraFX：修复了运动模糊弄坏某些车辆高速旋转车轮的问题，以及整体着色的几处修复；
*   ExtraFX：体积光不再需要雾模糊才能工作；
*   GamepadFX：修复了 Mobile 脚本，现在指向新的 Expo snack（旧的没法正常更新）；
*   键盘控制：车轮悬空时刹车应该有效；
*   修复了天文计算的精度，现在能对上日食之类；
*   修复了聊天设置的保存；
*   车辆生成时车轮朝向随机；
*   附近生成的刹车痕会基于局部高度图贴合到实际视觉网格；
*   修复了 OBS 集成；
*   阴影级联之间的过渡改进；
*   GrassFX：着色更新并修复，阴影更平滑；
*   CSP 预览配置 ZIP 可放到 `Documents\AC\cfg\extension`，CSP 会改用它；
*   修复了与最新版 ReShade 的兼容性；
*   车辆 Fake shadows 重投影更稳定；
*   截图时新树木在更远处也以更高细节绘制；
*   赛道扩展物理不再把 AI 弄坏；
*   车身柔性：与防倾杆兼容性更好；
*   车轮的 Fake shadows 与车身本身的 Fake shadows 对齐；
*   从 CM 启动时偶尔找不到回放的问题不应再发生；
*   若 data.acd 存在但缺少文件，AC 不再从 data 文件夹加载；
*   额外的 G/H/I/J 键盘绑定现在可用；
*   纹理加载更可靠，能再次正确处理损坏的 DDS 文件或生成缺失的 MIP；
*   修复了常规设置中的车辆相机复选框（某些组合禁用得太多）；
*   ExtraFX 自发光与数字 LED 合并兼容；
*   `smCarPaint` 着色器的弯曲版本能正确投影；
*   修复了网格压缩选项；
*   未编辑时尾翼不再在快速进站菜单中损坏（CSP 近期回退）；
*   即使网格有偏移，从碰撞体网格计算 AABB 也能工作（修复 CSP 预览等）；
*   修复了视频配置的实时重载；
*   修复了从聊天工具分配的自定义处罚；
*   调整了刹车盘的发热强度；
*   新更新的比赛默认赛道海拔设为 294 m；
*   BC7 纹理压缩（常规设置中的那两个优化选项）对 GPU 友好得多；
*   网格去重在 VR 中不再导致某些硬件卡死；
*   修复了 CSP 完整性校验（扩展服务器设置中的 `[EXTRA_TWEAKS] VERIFY_CSP_INTEGRITY=1`）；
*   修复了 AI 驾驶变差或选错 AI 线路的问题（CSP 近期回退）；
*   时区合理性修正更新；
*   YEBIS：把曝光设为 0 有效（画面会变黑，但总比忽略更改、切到 0% 并导致不可预测行为要好）；
*   除非窗口在前台，AC 不再移动鼠标指针；
*   修复了 Pro view；
*   INIpp 解析器能正确处理 `\r` 符号；
*   修复了导致 PaintShop 部分功能失效的几处回退；
*   关于 AC 物理循环迟到的消息不应再出现在 log.txt 中，节省一点时间；
*   修复了聊天消息的 TTS；
*   火花行为与观感的几项调整，ExtraFX 下发光更强；
*   TyresFX：修复了推测值不对称的问题，默认值考虑视觉车轮偏移；
*   自由相机的车辆聚焦判定距离从 3 米增加到 5 米；
*   雨天 AI 线路不再在某些赛道导致崩溃；
*   新车手名牌尊重旧版为聚焦车辆绘制名牌的选项；
*   后视镜 UV 归一化与 RainFX 配合良好；
*   AC 关闭过程现在更优雅；
*   几项让 AI 线路录制可用的修复；
*   扩展车辆物理：没有热 LUT 的轮胎恢复工作；
*   修复了车辆双闪的闪烁周期；
*   晃动的大灯随损坏晃得更厉害；
*   刹车痕粒子不再因分辨率变化而损坏；
*   若加载缓存的 AI 线路元数据出错，AC 不再崩溃，AI 线路更改后元数据也会正确更新；
*   lods.ini 中指向同一模型的多个条目会被合并为一个；
*   轮胎宽度缺失或为 0 不再导致 TyresFX 失效与轮胎网格消失；
*   从配置或 Lua 按名称引用车辆纹理现在不区分大小写；
*   轮胎检测线不再把墙体网格视为双面（几个版本前 CSP 的回退）；
*   修复了自定义 WeatherFX 样式下的焦散亮度；
*   配置 `yebisest.ini` 现在被正确忽略，该模块将始终激活；
*   启用 Autopilot 不再改变车内的油量；
*   禁用 Autopilot 会恢复自动换挡状态；
*   在 ext_config.ini 中使用 `lights.ksanim` 会禁用原版 AC 大灯动画；
*   截图时自动曝光停止更新；
*   赛道自定义物理：车辆随机摆放不再作用于第一辆车；
*   在 WeatherFX 样式中用自定义后处理替换 YEBIS 不再破坏不带后处理渲染 VR HUD 的选项；
*   VR Tweaks：边角遮罩优化不再干扰场景渲染；
*   NeckFX：车辆传送或掉头时，绘制车手头盔不再会乱几帧；
*   刹车痕（尤其是泥土刹车痕）在镜子里的观感好得多；
*   反射立方体贴图降采样的几项改进，反射更平滑；
*   修复了 App Shelf 布局；
*   修复了 Android Auto YouTube，现在以随机搜索之一启动（YouTube 主页不再展示视频）；
*   正确的 PBR 着色器（`fuPBR…`）已修复，亮度与反射率应正确（但要有最佳观感，仍需要使用线性色彩空间的 WeatherFX 样式）；
*   修复了雾中的折射大灯；
*   截图渲染比例与主渲染比例不同时，累积 AA 禁用与保存原始截图选项可正常工作；
*   排气火焰的光正确闪烁；
*   云上的阴影更柔和；
*   默认 WeatherFX 样式：对光污染强度做钳制，应对奇怪的配置；
*   移除了老旧而无意义的拉力调整（在拉力赛道上为单辆拉力赛车增强灯光参数）；
*   修复了关于 200 km/h 的 AI 调整（之前无法正确加载）；
*   若干小修复与优化；

## 配置

*   赛道表面：用可自定义的 perlin 噪声替换正弦噪声的选项；
*   在 surfaces.ini 中设置赛道重力现在对使用新版 CSP 的联机服务器生效（此外，海拔设到 20 km 以上也会改变重力）；
*   `data_human_materials.ini` 的实时重载；
*   新赛道选项 `[BOUNCED_LIGHT] CAUSTICS=SPECTATOR_ONLY`，性能更好；
*   Objects Inspector：为选中网格显示 AABB 与包围球的选项；
*   新的会话参数 `[EXTRA_RULES] RETAIN_BRAKES_TEMPERATURE_IN_PITS=1`；
*   离线比赛参数 `[RACE] FORCE_BASE_PHYSICS=1`，用于禁用扩展物理；
*   controls.ini 的新参数 `[STEER] SCALE_POSTPROCESS=1`；
*   在 tyres.ini 中使用 `[_EXTENSION] RIM_FRICTION_X=200 RIM_FRICTION_Z=100`，为爆胎车辆配置轮辋摩擦；
*   火焰设置使用 `…__MASK` 指定哪些火焰从哪些喷口喷出；
*   `smGlass` 着色器：修复磨砂观感，通过 `RefractionMatte = 1` 提供；
*   GrassFX：用 AO 添加额外自阴影的选项（需要带正确 AO 的纹理），使用默认草纹理的配置默认启用；
*   车漆材质的新参数 `UseNormalsAlphaToMaskFlakes`；
*   Android Auto：车辆可以为用户添加可供选择的新壁纸；

## Lua & Python

*   通用 API：
    *   `math.perlin()`，用于 1/2/3D perlin 噪声；
    *   新的 `ac.DataLUT21` 与 `ac.INIConfig:tryGet2DLut()`，用于处理 2D LUT；
    *   新字段：`ac.StateCar.dirt`、`ac.StateSim.weatherConditions`、`ac.StateSim.cameraExposure`、`ac.StateSim.exposureMultiplier`；
    *   修复了字段 `ac.StateCar.headlightsColor`；
    *   新的 `ac.convertHDRToLDR()`，将 HDR 值转换为 LDR，会考虑伽马或亮度倍率等线性色彩空间调整；
    *   新的 `ac.getCarOptimalBrakingAmount()`，计算安全制动所需的最佳刹车踏板输入；
    *   局部速度、角速度与局部角速度在回放中可用；
    *   用 `web.socket()` 连接 WebSocket；
    *   Web 函数使用正常的错误消息而非数字；
    *   `os.runConsoleProcess()` 正确处理独立的 stderr；
    *   从文件创建的音频事件：定位（seek）、获取总时长与时间线位置；
    *   创建音频事件组并向其添加 DSP，而非向实际音频事件添加（目前仅限从文件或流创建的音频事件）；
    *   修复了 `string.nextEmoji()`，新增 `string.codePointAt()` 方法；
    *   用 `carN::kn5::` 前缀从车辆 KN5 加载原始纹理（即使被涂装覆盖）；
    *   Scene API 替换纹理时保留原始纹理名称，让 `:applySkin()` 之类继续工作；
    *   `:applySkin()` 不再作用于从 Lua 加载的 KN5；
    *   修复了 `ui.addSettings()` 的图标与回调；
    *   多个脚本同时使用时，`ui.onExclusiveHUD()` 现在更可靠；
    *   用 `ac.collectKN5TextureNames()` 加载原始 KN5 纹理名称列表；
    *   用 `ac.collectKN5MaterialProperties()` 加载原始 KN5 材质；
    *   着色器模板已更新，为线性色彩空间做好准备；
    *   解析车辆或赛道配置时，可轻松设置包含文件的搜索位置；
    *   `stringify.binary` 可处理向量等，无论它们是引用还是指针或值；
    *   已加载的 KN5 文件不再因持有引用被垃圾回收而卸载；
    *   额外画布保存为 DDS 时现在也包含 MIP；
    *   额外画布的 `:accessData()` 响应更快；
    *   几何截图：YEBIS 顶层加入正确的 FXAA 3.11 抗锯齿并支持透明；
    *   几何截图：单独绘制车辆时可包含 Fake shadows，绘制整个场景时车辆 Fake shadows 也能画得好看；
    *   修复了 `ac.getAirPressure()` 与 `ac.getAirHumidity()`；
    *   `ac.ControlButton:boundTo()` 在按钮未绑定任何东西时正常工作；
    *   在 UI 绘制上下文之外调用 UI 函数应抛出错误，而不是让 AC 崩溃；
    *   `ui.UserIconsLayer()` 使用非默认列数时现在可用；
    *   `render.on()`：可监听更多事件名称；
    *   TTS：调节音量与语速、选择性别、完成时回调等改进；
    *   `ac.mediaCurrentPeak()`：参数也可监视麦克风峰值；
    *   新的 `table.same()`，快速可靠地比较表内容；
*   Gameplay API（应用、新模式、联机脚本）：
    *   新的 `ac.setExtraTrackLODMultiplier()`，用于相机飞越赛道之类的场景；
    *   新的 `ac.setBodyDirt()`；
    *   部分 Lua 应用 API，如重置车辆、保存与加载车辆状态、标记圈速无效、控制应用窗口、启动/重启/传送回维修站、屏蔽 Escape 按钮、设置车辆激活，已移入 Gameplay API；
    *   `ac.tryToOpenRaceMenu()` 也可以指定要打开的标签页；
    *   新函数 `ac.tryToSkipSession()`；
    *   AC 暂停时，新模式也会收到回调与计时器调用；
*   Physics API：
    *   新函数 `ac.resetColliderWears()`；
    *   `ac.disableCarCollisions()` 可在联机中使用；
*   Lua 应用：
    *   `ac.setExposureMultiplier()`，用于修改那个曝光滑块；
    *   `ac.getSetupSpinners()` 正确返回扩展条目的列表；
    *   用 `ac.accessAppWindow()` 也能移动 Lua 应用了；
*   车辆物理脚本：
    *   `ac.overrideBrakesTorque()` 的效果现在可以禁用；
    *   `ac.overrideSpecificValue()` 可覆盖另一些特殊值；
    *   用 `ac.addElectricTorque()` 为特定车轮添加额外电动扭矩；
    *   控制器输入数量从 8 增加到 256；
    *   在回放中记录控制器输入；
    *   添加额外质量不应再破坏车辆的位置重置；
    *   操作车轮与轮胎的函数现在也可以接受枚举，一次修改多个；
    *   `[CUSTOM_SCRIPT_ITEM_…]` 条目在未设置 ID 时使用节名；
*   WeatherFX：
    *   用 `ac.useLinearColorSpace()` 切换到线性色彩空间：
        *   一批随线性色彩空间激活的修复；
        *   更新了基于 IBL 的环境光；
        *   双层雾；
        *   用 `ac.setHDRToLDRConversionHints()` 提示 AC 如何快速把 HDR 图像转换为 LDR 图像；
        *   用 `ac.setWeatherLightsMultiplier2()` 为动态光源添加额外倍率，且不会使其停用；
    *   雾：可为不同颜色通道设置不同的大气吸收（可选）；
    *   为更好看的地平线（以及从太空看地球）设置地球纹理的选项；
    *   新的 `ac.getSunMoonAngle()`，用于精确估算日食；
    *   新的 `ac.pauseCubemapUpdates()`；
    *   `ac.setWeatherTimeOffset()` 偏移足够大时可启用真实太阳轨迹；
    *   只有太阳真正位于地平线下时阳光才停止，而不是 Y 分量小于 0 就停止；
    *   后处理回调收到的默认 filmic contrast 值恢复为应有的 0.5；
*   新的共享库：
    *   调整已存车辆状态（例如可用于实现保留动量的传送门）；
    *   访问部分聊天函数；
    *   访问部分 Steam API；
    *   从自定义点发射烟花；
*   保存文件会基于某些文件的存在与否为 Lua 脚本重置缓存；
*   在 Lua Debug 应用中将后台 Lua worker 在完成后保留一段时间的选项；
*   LuaJIT 已更新至最新上游改动；
