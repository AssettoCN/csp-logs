---
title: 首页
tableOfContents: false
---

import versionsData from '../../data/versions.json';

export const allVersions = versionsData.groups.flat();
export const latestPublic = allVersions.find((v) => !v.isPreview);
export const latestPreview = allVersions.find((v) => v.isPreview);

Custom Shaders Patch 更新日志归档，收录曾通过 Patreon、acstuff.club 或 CSP Discord 公开发的各版本。

目前已收录 **{versionsData.counts.total}** 个版本的更新日志，其中 **{versionsData.counts.previews}** 个为 **Preview（预览）** 版本，**{versionsData.counts.releases}** 个为 **Public（公开）** 版本。
最新 **公开** 版本 <a href="/csp-logs/latest/public">{latestPublic.versionName}</a>，最新 **预览** 版本 <a href="/csp-logs/latest/preview">{latestPreview.versionName}</a>。

**Preview** 版本仅对 [x4fab 的 Patreon](https://www.patreon.com/x4fab) 会员开放，通常包含更多功能，尤其是 RainFX 模块。
**Public** 版本可通过 [acstuff.club/patch](https://acstuff.club/patch/) 或 [Content Manager](https://acstuff.club/app/) 免费获取。
